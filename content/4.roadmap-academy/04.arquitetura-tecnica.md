---
title: Arquitetura técnica
description: Stack, o pipeline de conteúdo gerado (sem CMS), autenticação e progresso no Supabase e o deploy SSR na Cloudflare.
icon: lucide:layers
type: referencia
---

O RoadMap Academy é um **app Nuxt 4 com renderização no servidor (SSR)**. O conteúdo do
curso é **estático e gerado por scripts** a partir de arquivos-fonte; o Supabase guarda
apenas dados de usuário; o deploy roda como Worker na Cloudflare.

# 1. Stack

| Camada | Tecnologia |
| --- | --- |
| Framework | **Nuxt 4** (Vue 3) em modo SSR |
| Estado | **Pinia** |
| Conteúdo | Estático em `app/data/*.ts` → fatiado em JSON por aula (não usa CMS) |
| Auth + progresso | **Supabase** (`@nuxtjs/supabase` v2) |
| Playground | **Vue Flow** (`@vue-flow/core` + background/controls/minimap) |
| Diagramas | **Mermaid** v11 (render client-side) |
| Realce de código | **highlight.js** + markdown-it |
| Deploy | **Cloudflare Workers** (nitro preset `cloudflare_module`) |

::callout{type="info" title="Por que não um CMS?"}
As aulas não vêm de um banco nem de um headless CMS. Elas são **arquivos-fonte** processados
em tempo de build para estruturas TypeScript e, depois, em arquivos JSON servidos sob demanda.
Isso mantém o conteúdo versionado no Git, o site rápido (conteúdo estático) e o banco enxuto
(só dados de usuário).
::

# 2. O pipeline de conteúdo

Esta é a peça mais característica do projeto. O conteúdo passa por uma cadeia de scripts
(`scripts/*.mjs`) antes de virar site:

::steps
### Fonte
As aulas vivem em `content/` organizadas por trilha e módulo (`content/trilha-*/modulo-*/`)
e por projeto (`content/projetos-praticos/`).

### Geração
`gen-data.mjs` monta os metadados (módulos, aulas, ordem); `gen-content.mjs` monta o corpo
das aulas; `gen-projetos.mjs` faz o mesmo para os projetos. A saída são arquivos TypeScript
em `app/data/` (`modules.ts`, `lessons.ts`, `content.ts`, `project-*.ts`).

### Fatiamento
`split-content.mjs` quebra o conteúdo em um JSON por aula em `public/content/{id}.json`, para
o viewer buscar só a aula aberta em vez de carregar tudo.

### Extras
`gen-extras.mjs` gera, a partir do conteúdo, o índice de busca, o banco de entrevistas e o
sitemap.
::

::callout{type="warning" title="Regra de ouro do repositório"}
Os arquivos em `app/data/` (`modules.ts`, `lessons.ts`, `content.ts`, `project-*.ts`) são
**gerados** — nunca editar à mão, pois qualquer alteração é sobrescrita ao rodar os scripts.
A fonte da verdade é a pasta `content/`. Depois de mexer nela, rode a geração novamente
(os comandos `dev`/`build`/`generate` já disparam o pipeline).
::

O `package.json` amarra tudo: `dev`, `build` e `generate` rodam `split-content` + `gen-extras`
antes do Nuxt. Há ainda scripts de qualidade — `validate:code` (valida sintaxe dos blocos de
código das aulas) e validadores de estrutura.

# 3. Autenticação e progresso (Supabase)

- **`@nuxtjs/supabase` v2** com `redirect: false` — a proteção de rota é manual (não global),
  então o grosso do site é público (bom para SEO) e só `dashboard`, `perfil` e `certificados`
  exigem login (via middleware `auth`).
- **Schema** (`supabase/schema.sql`, idempotente): tabela `profiles` (criada por trigger no
  signup) e `lesson_progress` (`user_id`, `lesson_id`, `completed_at`), ambas com **RLS por
  dono**.
- **Progresso híbrido** (`useProgress.ts`): Supabase quando logado, `localStorage` quando
  anônimo, com merge no login — o aluno pode começar a estudar sem criar conta.

# 4. Deploy (Cloudflare)

- Nuxt SSR pelo nitro preset **`cloudflare_module`**, com saída em `.output/`. Requer a flag
  **`nodejs_compat`** (declarada no `wrangler.toml`).
- Três variáveis de ambiente no painel: `SUPABASE_URL`, `SUPABASE_KEY` (anon) e
  `NUXT_PUBLIC_SITE_URL`. Nada de segredo é commitado (`.env` é gitignored; há `.env.example`).
- Publicação via `npx wrangler deploy` ou Workers Builds. O passo-a-passo completo mora no
  `DEPLOY.md` do repositório.

::callout{type="note" title="Nota de build (histórico)"}
O `package-lock.json` **não é versionado** de propósito. Dependências nativas opcionais do
Nuxt (bindings por plataforma) fazem o `npm ci` estrito quebrar no Linux da Cloudflare quando
o lock foi gerado no Windows. Sem lock versionado, o build usa `npm install`, que resolve as
dependências certas por plataforma. Recomitar o lock quebra o deploy.
::

# 5. Código-companheiro

Cada projeto prático tem um **scaffold de referência** em `companion-code/` — a maioria são
apps duais Spring Boot 3 / ASP.NET Core 8; o projeto 03 é um conjunto de microsserviços
(gateway + serviços + RabbitMQ), o 07 é a montagem Strangler Fig (legado + serviço novo +
nginx) e o 09 é um RAG com agente ReAct e cliente LLM provider-agnóstico. Servem como
gabarito de arquitetura, ligados a cada projeto na UI.

::callout{type="info" title="Sobre os scaffolds"}
Os scaffolds são **referência de arquitetura**, declarados como não-compilados no próprio
README de cada um — pensados para o aluno ler e reconstruir, alinhados às fases das aulas.
::
